export const frontmatter = {
  title: 'Docker Compose for Development Environments',
  date: '2026-01-10',
  excerpt: 'Setting up reproducible development environments with Docker Compose. Covers multi-service setups, hot reloading, database seeding, and production-like configurations.',
  thumbnail: 'https://images.unsplash.com/photo-1728667797688-7fab9401d0f2?q=80&w=1632&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D',
  tags: ['Docker', 'DevOps', 'Development', 'Tooling'],
}

## Docker Compose for Development Environments

Consistent development environments across team members eliminate the "it works on my machine" problem. Docker Compose makes this straightforward.

### Basic Setup

A typical web application setup with PostgreSQL and Redis:

```yaml
version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/app
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: app
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'

volumes:
  pgdata:
```

### Hot Reloading

For Node.js applications, use a volume mount with nodemon or tsx:

```yaml
services:
  app:
    command: npx tsx watch src/index.ts
    volumes:
      - .:/app
```

The key insight: mount your source code into the container so changes are reflected immediately, but use an anonymous volume for `node_modules` to preserve the container's installed dependencies.

### Database Seeding

Create a separate service for seeding:

```yaml
services:
  seed:
    build: .
    command: npx prisma db seed
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/app
```

### Multi-Stage Builds for Production

```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS runner
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
EXPOSE 3000
CMD ["node", "dist/main.js"]
```

### Conclusion

Docker Compose gives you reproducible environments with minimal configuration overhead. Start with a simple setup and add services as your architecture grows. The investment pays off every time a new developer joins the team.
